# PRD 智能看板 · 交接文档

> 版本：v17.17（多项目总览）｜ 交接日期：2026-08-16     交接人：智能设计助手 ｜ 接收人：座舱产品实习生（跨电脑继续任务）

---

## 一、一句话概况

**PRD 智能看板** 是一个单文件 HTML 的 PRD 撰写 + 健康度自检工具：14 节标准框架、红黄绿体检、Word 导入导出、划线评论、富文本表格、项目分组收纳，纯前端本地存储，无需服务器。

## 二、如何打开使用（最重要）

### 方式 A：任何电脑直接访问（已部署）

**分享链接：https://614c688426cb4c8ea2937b2b0e22cf1e.app.workbuddy.link**

- 任意电脑浏览器打开即用，无需安装任何软件；

- 数据存在**当前浏览器**的 localStorage 里——**换电脑/换浏览器数据不互通**，这是单机工具的固有限制；

- 想在新电脑上继续旧数据：在旧电脑浏览器里用「顶栏 · 更多 → 导出 JSON 备份」，到新电脑再用「导入 JSON 备份」恢复。

**你可以从设置菜单管理这个已发布的应用（例如删除它）：设置 - 数据管理 - 我发布的应用。**

### 方式 B：本地文件

主文件：`D:\虚拟看板\PRD智能看板.html`（约 2.1MB，双击用浏览器打开）。

> ⚠️ 目录里还有几个旧文件**不要用**：`PRD智能看板-深色拟态.html`（8/13 旧版）、`PRD智能看板_原始备份_20260813.html`（备份）、`publish\index.html`（旧发布）。认准主文件名 + 打开后看**左下角版本水印**必须是 `v16.1 · 项目下拉弹窗`。

## 三、当前功能清单（v16.1）

| 模块 | 功能 |
| --- | --- |
| 项目与分组 | 「项目」按钮下拉弹窗；分组创建/展开折叠/重命名/删除；项目拖拽进组、组内拖拽排序、拖到"未分组"移出 |
| 框架 | 14 节标准框架（目的/功能需求/验收/用户故事/埋点/变更历史…）、自定义框架、节拖拽排序、框架管理页 |
| 健康度体检 | 逐节红黄绿规则校验、完成度/风险/一致性指标、缺口清单点击跳转、大纲滚动高亮 |
| 正文编辑 | 富文本（加粗/斜体/标题/链接迷你工具条）、插入图片/表格、富文本表格 +行/+列/-行/-列 + 行尾列头 × 删除 |
| 评论 | 划线评论、气泡常驻、顶栏「评论」聚合面板、点击跳转 |
| AI 助手（v17.15） | 设置页「AI」tab（服务商可换智谱 GLM-4-Flash 等免费模型：Base URL/模型/Key 均可配置、Key 仅存本机、可另配复检模型、测试连接）；6 维健康度（权重可调、temperature 0、每条问题带原文引用并做幻觉标记、同文档评分缓存）；一键优化（全文/按节、块级最小编辑、独立复检 + 确定性校验、护栏、规则引擎前后对比、大文档直接按节优化）；**AI 结构对齐（v17.2+）**——导入错位自动提示，AI 提议块/行跨节搬移 + **自动执行改名/删空节/合并/拆分（v17.3）**（逐条确认、校验不过禁接受、生成「结构对齐」版本可一键撤销搬回原位）；**AI 撰写（v17.15）**——输入产品/功能描述，AI 新建项目并按框架逐节生成整份草稿（文本/表格/功能/验收/用户故事全类型，Markdown→HTML 本地转换、字段校验归一、单节失败自动跳过并诊断），逐条确认后写入、版本可回滚（恢复=回到生成前空白，可经恢复前快照找回）；**停止按钮（v17.3）**——AI 运行中可随时中断（中止请求、不写入、可重试）；**导入默认自动框架（v17.3）**——无项目/空项目导入按文档标题自动建框架，不再硬套 14 节；版本链兼容新旧补丁格式（含框架级改名/增删节补丁与 `items` 清单补丁），恢复前自动存安全快照 |
| 导入导出 | Word(.docx) 导入自动分类、导出 Word/MD、JSON 备份/恢复、规则/框架导入导出、模板 |
| 看板总览（v17.16） | hero 实时摘要（完成度/风险/AI 总评/更新时间）；节健康度热力图（每节一个色块，绿/黄/红，点击定位）；AI 总评卡（总分+6 维迷你条）；体检摘要一键复制 Markdown（含逐节状态与缺口清单，粘贴评审群用）；AI 面板工具行等宽 + 头部渐变 |
| 多项目总览（v17.17） | 侧栏「◫ 总览」浮层：所有项目健康卡横向对比（完成度/红节/AI 总评/逐节迷你色点/更新时间），顶部统计（平均完成度/有风险项目数/AI 总评数），点击卡片切换项目；自动框架节按标题关键词补必填语义（导入文档不再恒 100%）；视图态报告横幅增加风险红节数与 AI 总评 |
| 其他 | 撤销（保护项目操作）、密度切换、深色/浅色主题 |

## 四、技术要点（继续开发必读）

- **单文件自包含**：全部 HTML/CSS/JS 在 `PRD智能看板.html` 一个文件里，共 7 个 `<script>` 块（block0 主题 / block1 主逻辑 / block2 视图模式 / block3-4 Floating UI（MIT 内联）/ block5 评论控制器 / block6 AI 控制器 `id="ai-controller"`）。AI 功能全部在新块内，不改 block1。

- **存储**：localStorage，键 `STORAGE_KEY`；`STATE` 含 `projects[]`、`groups[]`（分组）、`groupOpen{}`（分组展开态）、`framework[]`、`ruleSet[]`、`frameworkPresets[]`。

- **数据模型**：项目 `p.groupId` 挂分组；节数据 `DATA[secId]`；表格节用 `rows` 数组；富文本表格存在正文 `html` 里（无独立数据结构）。

- **富文本表格操作条**：编辑态渲染后注入（`injectRichTableBars`），保存时用 `stripRtblHtml` 剥离——改保存/渲染逻辑时必须同步 3 处，否则操作条会污染数据。

- **版本水印**：左下角 `<div id="vbadge">`，每次发版改它，用户刷新后一眼确认加载的版本。

- **AI 存储隔离**：API Key 存独立 localStorage 键 `prdKanbanAiSettings`（**不进入 STATE、不随 JSON 备份导出**）；AI 业务数据（评分报告/问题忽略/待确认 Diff/版本链）存**项目对象 `p.ai`**（随项目走，备份含）。版本用**节级差异补丁**（`{sectionId:{old,new}}`，old/new 逐字快照），不存全文快照。

- **验证手段**（本地开发时）：`D:\虚拟看板\tools\` 下有回归测试脚本（verify\_docx.js / \_verify\_export.js / \_verify\_cmt3.js / \_verify\_r13.js / \_verify\_r24.py 等），改完跑一遍 + 浏览器里对照水印自测。

## 五、迭代历史速览（给接手人的背景）

| 版本 | 内容 |
| --- | --- |
| v13 | 修撤销误删项目、表格增删行列兜底、列宽拖拽（table-layout:fixed） |
| v14 | 表格内容丢失（补 data-sec）、删除此列、评论点击修复、顶栏评论/插入按钮 |
| v15 | 产品优化-Harness 闭环：富文本表格操作条（v15.6 架构级重做，绕开 hover 直接注入）、行/列 × 删除、迷你格式工具条（P4） |
| v15.9 | M2 节拖拽排序 + M4 大纲滚动高亮 |
| v16.0 | 项目分组收纳（分组/展开/重命名/删除/拖拽） |
| v16.1 | 分组收进「项目」按钮 + 下拉弹窗 |
| v16.2 | 安全修复：①自定义规则真正生效（keyword 此前是死代码，scope=全部节 曾存成 ['all'] 被过滤）；②正文内 Ctrl+Z 走浏览器原生撤销，不再清空刚输入内容；③localStorage 写满时顶部红色告警 + 一键导出备份，不再静默丢数据；④富文本正文/卡片输入即防抖保存，不再只依赖失焦 |
| v16.3 | 判分规则冻结为 12 条内置基线（删除 R-SPEC-04/R-CONS-05/R-CONS-06/R-TEST-03/R-TEST-04/R-SAFE-01，合并 R-CONS-01/02，规则设置页改只读清单）；判分按「节类型/标题」定位不再写死节 id；表格节 rows 纳入 isEmpty/plain（修复表格节误报"必填为空"与相关死规则）；验收"待定"不再标黄、只红"不通过"；R-RISK-02 阈值 30→40；保留忽略/已订正 + 手动改色人工审核通道 |
| v16.4 | 表格交互改为 Excel 式右键菜单：行间/列间插入（上方/下方/左侧/右侧）、删行/删列/删表；移除富文本表格常驻操作条与单元格 × 按钮（保留历史数据剥离）；修复结构化表格缩放失效（contenteditable 文本节点导致 closest 崩溃）+ 扩大拖拽热区；评论气泡锚定划线、划出视口自动隐藏（修复气泡钉在页面最底端） |
| v16.5 | 结构化表格与富文本表格统一：移除操作列/行内 × /底部追加按钮，增删改全部走右键菜单；评论气泡改为始终可见——划线在视口内贴线显示，划出视口时贴到对应边缘并显示方向提示（↑/↓），不再消失也不钉死在底端 |
| v16.6 | 结构化表格拖拽重做：弃用内容单元格边缘热区（误触/灵敏度过高），改为显式手柄——表头右缘 7px 拖列宽、行首格下缘 7px 拖行高，松手即落盘；旧边缘热区仅保留给富文本表格。评论气泡改用内联的 Floating UI（MIT）锚定：offset/flip/shift/hide + autoUpdate 自动跟随滚动/缩放，划线划出视口气泡自动隐藏、滚回自动恢复，消除"游离到侧栏/底端"的手写定位问题 |
| v16.7 | ①结构化表格点击不再弹浮动工具条/右下角整体缩放角标（`selectMedia` 对 `.table-scroll` 提前返回），列宽/行高只走各自手柄；②评论气泡"看不到"根因修复：CSS 中一条顶层 `.cmt-tip{position:relative}` 覆盖了 `position:fixed`，导致 Floating UI 的 fixed 坐标全部错位、气泡渲染到文档底部视口外——已删除该覆盖，并给 `computePosition` 显式传 `strategy:'fixed'`、增加初始就近放置与 Promise catch 兜底。已用无头 Chrome 真机验证窄/宽两种视口气泡均可见 |
| v16.8 | 修复"插入表格"弹窗网格挤成一团：JS 之前把 100 个按钮包进 10 个行 div，而 `.table-modal-grid` 的 CSS 是按 100 按钮平铺 10×10 写的，两层布局冲突导致按钮被压缩；改为 JS 平铺 100 按钮 + 点击从按钮读取行列，网格居中并允许窄屏横向滚动 |
| v16.9 | 富文本表格也取消整体缩放：右下角角标（`imgResizeHandle`）不再对 TABLE 显示，浮动条移除「拖拽缩放」按钮（仅图片保留尺寸预设）；富文本表格列宽/行高仍走单元格边缘拖拽与右键菜单 |
| v17.0 | AI 助手（方案 C 纯前端，DeepSeek 直连）：设置页第 4 tab「AI」（Key 独立键、测试连接、CORS 分类报错）；6 维健康度加权评分 + 逐条诊断（可定位/忽略/已订正）；一键优化 Agent 工作流（全文/按节、复检、目标分与轮数护栏、分数回退自动保留最好版本、SSE 流式 + 非流式降级）；AI Diff 逐条确认后才写入；节级差异补丁版本链（上限 10、恢复前安全快照、倒序逐字回放精确回滚）；UI/导出无 AI 内容标记；规则引擎与评论/表格等旧功能零回归（6 套回归 111 断言 + 无头 Chrome 端到端全绿） |
| v17.1 | 可信度升级：①评分校准——评分/复检 temperature 0、每条问题带原文引用 `quote` + 维度 `note`、引用未匹配自动标「疑似幻觉」、内容指纹评分缓存（同文档零重复调用）；②独立复检 `aiReview`（独立 prompt + verdict pass/needs_work/fail，可另配 reviewModel）+ 确定性校验（HTML 标签闭合/表格列数一致/评论划线完整性/引用匹配/越界修改，校验不过禁接受）+ 规则引擎前后对比（恶化仅警告）；③最小编辑——AI 输出块级 edits（replaceBlock/insertBlock/deleteBlock，match 逐字引用原文）与表格行级 rowEdits（按首列定位），未引用内容一律不动；版本补丁改为块/行级（新旧双格式兼容，v17.0 版本链可继续查看/恢复）。6 套回归 117 断言 + 无头 Chrome 端到端 9 断言 + 评论/网格旧检查全绿 |
| v17.2 | AI 结构对齐：新增错位检测 `aiAlignHint`（兜底节内容占比/必填节空/单节块数异常，本地确定性信号）+ 导入后 hook（包装 `doImportText`，高风险才 toast 提示、不自动执行）；AI 提议 `moveBlock`/`moveRow` 跨节搬移（match 逐字引用、text↔text 与 table↔table、表格列数校验、锚点匹配，校验不过禁接受）+ 改名/合并/拆节建议文字；复用 Diff 确认交互，应用后生成「结构对齐」版本（双节补丁：来源=删除块、目标=插入块），变换型版本「恢复」=撤销搬移搬回原位；实测智谱 GLM-4-Flash 可浏览器直连（CORS 放行）作为免费模型接入。6 套回归 129 断言 + 无头 Chrome 端到端 13 断言全绿 |
| v17.3 | 三项体验升级：①**停止按钮**——`aiGlobalAbort/aiCancelFlag` 全局中止（AbortController 联动每次请求 + 重试/轮询断点），运行中面板出现「■ 停止」，中断后不写入、可重试；②**导入默认自动框架**——包装 `doImportText`：无项目/空项目导入时 `createProject` + `autoGenImport`（按文档标题建框架），不再硬套 14 节；③**对齐自动调整**——AI 输出 `ops`（rename/deleteEmpty/merge/split）真正执行：框架级补丁 `fwname/fwdel/fwadd`（`aiFwRename/aiFwRemove/aiFwInsert`），`aiReverseVersionPatch` 统一倒序还原（框架条目先行+节条目），恢复=撤销改名/删节/合并/拆分；仅内容为空且非必填才能删空节、仅同类型节可合并。6 套回归 140 断言 + 无头 Chrome 端到端 18 断言全绿 |
| v17.4 | **导入目录/标题识别修复**（真实案例：贷款需求文档 50 个真实标题被撑成 290 节 → 修复后 72 节）：①`headingLevelOf` 移除"任何带字母的样式都按二级标题"的误判（Word 自动目录样式 TOC1-TOC4、列表/正文样式不再当标题），outlineLvl 只认 0-8（9=正文），加粗兜底排除封面日期/编号行；②`parseAutoGen` 跳过 Word 自动目录块（「目录」→第一个真实 `#` 标题），且文本中只要有 `#` 标题就禁用"编号行/已知节名"兜底，正文编号列表行不再误判为节。6 套回归 142 断言全绿；真实 docx 导入验证 290→72 节 |
| v17.5 | 四项体验修复：①**左侧目录可滚动**——`#sidebar` 加 `max-height:calc(100vh-61px);overflow-y:auto`（长目录不再被截断划不动）；②**大标题→子标题层级**——加粗子标题降为三级 `###`，`parseAutoGen` 在有 `#/##` 大标题时把 `###` 归入父节成为小卡片（真实 docx：72 节 → 18 个顶级节，机构/角色管理、全部贷款/初审/授信等全部成为父节卡片；纯加粗文档无大标题时 `###` 仍作节兜底）；③**优化兼容整节返回**——模型若返回整节 `newHtml/newRows` 而非块级 edits，自动降级为整节替换（补丁 `kind:'section'/'rows'`，可回滚），并新增失败原因提示（引用不逐字/结构不完整/校验不过的原因 toast 与面板可见）；④**对齐 blocked 原因可见**——校验不过的调整项现在显示具体原因（如"目标表列数不一致"），不再"点击没反应"。6 套回归 150 断言 + 无头 Chrome 端到端 18 断言全绿 |
| v17.6 | **优化兜底重试 + 诊断可见**：①`aiOptimizeSimple` 简化整节替换 prompt——复杂"块级 edits"协议返回空/被过滤时自动改用整节替换重试一次（真实免费模型 GLM-4-Flash 对复杂 JSON 协议服从性差，这是"一直未产生修改建议"的主因）；②新增 `p.ai.lastOptDebug` 诊断（每次优化的模型返回条数/摘要、被过滤/blocked 原因、复核结论、简化重试步骤、错误信息），面板"最近一次优化诊断"可见可清除；③失败提示更具体（区分"模型未返回任何建议"与"返回 N 条但格式不符"）。6 套回归 153 断言 + 无头 Chrome 端到端 18 断言全绿 |
| v17.7 | **引用匹配修复**（用户诊断：模型返回 9 条但全部"未找到匹配的原文块"）：①`aiApplyEdits` 支持**连续多块范围匹配**（模型把连续多段粘贴成 match 时，按块序拼接匹配并整段替换）；②优化 prompt 的文档文本**不再包含小卡片内容**（`aiSecText(...,noCards)`/`aiDocTextOpt(true)`，模型不会引用到正文块之外的内容）；③**全部 blocked 时自动改用简化整节重试一次**（`blockedRetry` 记录进诊断）。6 套回归 158 断言 + 无头 Chrome 端到端 18 断言全绿 |
| v17.8 | **原文点击跳转**：AI 面板中所有"原"预览（优化 Diff 的块/整节/表格行、结构对齐的搬移内容、版本 Diff 的块/行/整节）均可点击——`aiJumpToBlock(sid,match)` 定位到对应节并高亮具体块/行（`.ai-flash` 闪烁 2.4s），找不到精确块时定位到节；表格节按首列匹配行。6 套回归 158 断言 + 无头 Chrome 端到端 19 断言全绿 |
| v17.9 | **逐条接受即写入 + 单条撤销**：此前"接受"只打标记、需全部确认才一次性写入（单点显得没反应）。现改为：①点某条「接受」/保存「修改」**立即写入正文**（`aiApplyDiffItemNow` 即时应用并记入 `pd.appliedPatches`），toast 提示还剩几条待确认；②已接受/已修改条目出现「撤销」按钮，一键反向应用该条恢复原文（`aiUndoDiffItem`）；③「全部接受」逐条应用后归档版本；版本仍由 `appliedPatches` 汇总生成、可回滚。6 套回归 162 断言 + 无头 Chrome 端到端 19 断言全绿 |
| v17.10 | **数据自动备份与恢复**：①包装 `save()`——每次保存 5s 防抖后把主存储复制到 `prdKanbanStateV3.bak`，`beforeunload` 兜底再写一次；②启动时 `aiRecoverFromBackup()`——主存储缺失（被清/被删）自动从备份恢复并提示；主存储存在但项目为空时**不自动覆盖**（尊重主动删除），面板显示「恢复备份」一键入口；③恢复后继续正常使用，备份随每次保存滚动更新。说明：本地存储按"浏览器+页面来源"隔离，换路径/换浏览器/清站点数据都会造成"数据不见了"，备份与恢复只能在同一存储空间内兜底，跨环境请用「导出 JSON 备份」。6 套回归 165 断言 + 无头 Chrome 端到端 19 断言全绿 |
| v17.11 | **新示例 PRD**：用户提供《智能座舱语音助手·多意图连续对话与免唤醒交互》需求文档，内嵌为新的示例文本（`AI_SAMPLE_TEXT`，约 7KB）；「加载示例」改为走**自动框架**（`autoGenImport`），示例按文档标题建节并保留层级（目的/适用范围/产品信息与目标…），不再用旧的固定 `sampleData`。6 套回归 167 断言 + 无头 Chrome 端到端 19 断言全绿 |
| v17.12 | **单个接受即时重绘正文（修复）**：v17.9 起单条「接受」已立即写入数据，但只刷新健康度角标、未重绘正文，编辑器仍显示旧文本（"全部接受"因归档流程末尾 `render()` 才看起来有效）。修复：单条接受/保存修改/撤销后均调用 `render()` 重绘正文（`aiDecideDiff`/`aiSaveModifiedDiff`/`aiUndoDiffItem`/`aiAcceptAll`）；浏览器端到端新增"单个接受：正文立即变化（数据+可见DOM）并归档版本"断言。6 套回归 167 断言 + 无头 Chrome 端到端 19 断言全绿 |
| v17.13 | **优化输出加固 + 按节兜底**（用户诊断"AI 返回内容不是合法 JSON，已重试"）：①`aiAskJSON` 改为**非流式 + `response_format:json_object` 优先**（弱模型更稳），流式作兜底；②请求增加 `max_tokens`（评分/复检/整份优化/简单优化/分节优化分级限制，降低长输出截断概率）；③**整份优化返回无法解析时自动按节逐个优化**（`aiOptimizeSafe`→`aiOptimizeBySection`，只处理有问题节、每节一个短请求，整节替换协议），合并后照常校验/复检/出 Diff；④解析失败把**原始返回内容前 400 字**存入诊断。修复过程发现并规避了二次归一化把 `replaceSection` 丢弃的 bug。6 套回归 169 断言 + 无头 Chrome 端到端 19 断言全绿 |
| v17.14 | **大文档直接按节优化**：整份文本 >2500 字符时跳过整份调用（弱模型必截断），直接 `aiOptimizeBySection` 按节逐个优化，诊断记录 `fullDocSkipped`；6 套回归 169 断言 + 端到端 19 断言全绿 |
| v17.15 | **AI 撰写草稿**：补齐"从零起草"——输入产品/功能描述 → 新建项目 → 按框架逐节生成（`aiGenSection`：text/timeline 走 Markdown→`aiMdToHtml`，table 走 `rows` 列数校验，feat/accept/users 走 `items` 字段归一），产出进 `pendingDiffs`（`gen:true` 显示「AI 撰写草稿」）逐条确认；新增 `kind:'items'` 版本补丁（old/new 逐字快照可精确回滚）；草稿版本 `transform:true`，恢复=回到生成前空白，恢复前自动存「恢复前快照」可找回草稿；新增 `regress_v1715.js`（22 断言）+ `browser_check_gen.mjs`（5 断言） |
| v17.16 | **看板总览升级**：hero 实时摘要；节健康度热力图（GitHub 风格色块网格，点击定位）；AI 总评卡（有报告显示总分+6 维条，无报告给体检入口）；体检摘要一键复制 Markdown（含逐节 emoji 状态与缺口清单）；AI 面板工具行等宽/头部渐变/滚动条美化；全组件深浅色自适应。新增 `browser_check_dash.mjs`（8 断言）；6 套回归 170 + v17.15 22 断言全绿 |
| v17.17 | **多项目总览 + 自动框架必填语义**：侧栏「◫ 总览」浮层横向对比所有项目健康卡（完成度/红节/AI 总评/逐节迷你色点），点击卡片切换项目；`healthForProject` 临时切换后恢复，不污染当前状态；`autoGenImport` 按标题关键词补 `required`（修复导入文档完成度恒 100% 的缺陷）；视图态报告横幅加风险红节与 AI 总评。`browser_check_dash.mjs` 增至 14 断言；6 套回归 170 + v17.15 22 断言全绿 |

## 六、遗留事项 \& 下一步候选（按优先级）

1. **M5 轻量 AI 辅助**：**已落地（v17.15 AI 撰写草稿 + v17.16 AI 总评卡）**——用户自配 API Key，输入描述即可生成整份草稿，逐条确认后写入；仍可补：当前项目空节补全模式、生成后自动跑深度体检闭环、按模板风格约束生成。

1. ~~**P5 健康度一键复制**~~：**已实现（v17.16）**——体检摘要复制为 Markdown 贴评审群。

1. **富文本表格列宽不持久化**：行/列操作后列宽不保存，刷新丢失（下轮可补 colWidths 存储）。

1. **评论气泡边缘场景**：极端快速操作下气泡恢复可能有轻微闪烁。

1. **M3 模板库**：内置 2-3 套 PRD 模板一键套用。

## 七、在新电脑上继续任务的操作指引

1. 打开分享链接或本地文件，确认左下角水印为 v17.17；

1. 若旧数据在另一台电脑：先「导出 JSON 备份」→ 新电脑「导入 JSON 备份」；

1. 需要继续开发：把 `D:\虚拟看板\PRD智能看板.html` 拷到新电脑，用编辑器改同一个文件（保持单文件结构，别拆散）；

1. 改完验证：跑 `tools\` 下回归脚本 + 浏览器自测关键路径（建项目→写正文→插表格→评论→分组拖拽→导出）；

1. 发版必做：更新左下角版本水印 v17.x，避免"不知道加载的是哪个版本"。

---

*备注：行业指标（如误唤醒率）参考厂商宣传口径，须自行实测校准；交付文档本文件为 UTF-8 无 BOM 编码，请用 WorkBuddy 预览或 VS Code 打开。*
